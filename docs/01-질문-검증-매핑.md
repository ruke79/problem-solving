# 면접 질문 ↔ 검증 케이스 매핑 (Q31~Q115)

업로드된 답변 스크립트 Part 3~7(Q31~Q115, 85문항)을 세 갈래로 분류했다.

| 분류 | 문항 수 | 의미 |
|---|---|---|
| **A. 실행 검증됨** | 43 | 이 랩에서 코드로 재현해 판정까지 남긴다 |
| **B. 이론 전용** | 26 | 명제가 관측 가능한 결과로 갈리지 않는다(조직·판단·설명형) |
| **C. 인프라 필요** | 16 | Kafka·Redis·K8s·GPU 가 있어야 진짜 검증이 된다 |

> **[2026-08-12] 실물 인프라(PostgreSQL 16 + Kafka 3.9)가 들어오면서 분류가 움직였다.**
> Kafka 쪽: `Q41`(C→A), `Q42`·`Q43`·`Q45`·`Q100` 이 인메모리 모사에서 **실물 검증**으로 승격됐다
> (`verify-labs-kafka` 모듈, `KAFKA-01`~`KAFKA-05`).
> `Q47 파티셔닝`(B→A), `Q51 CDC`(C→A) 가 실행 검증으로 넘어왔고,
> `Q40·Q73`(DDL 락), `Q61·Q100`(멱등성), `Q97`(write skew) 의 검증 깊이가 올라갔다.
> 새로 만든 케이스 5개는 `docs/08-PostgreSQL-로-늘어난-검증-범위.md` 참고.

> **[2026-08-12] Q1~Q200 원고를 전부 받았다.** 이 문서의 분류표는 Q31~Q115 기준이고,
> 새로 받은 범위(Q1~Q30, Q116~Q200)의 대조 결과는 `docs/09-테스트-누락-점검-Q1-Q200.md` 에 있다.

---

## A. 실행 검증되는 질문 (케이스 52개)

### Part 4 — MSA·분산 아키텍처

| Q | 질문 요지 | 케이스 | 검증 내용 |
|---|---|---|---|
| Q31 | 모놀리스→MSA 최대 과제 | `MSA-01` | Outbox 없으면 커밋 후 발행 실패 시 이벤트 영구 유실 / Outbox 는 릴레이로 복구 |
| Q37 | 결과적 일관성 구현 | `MSA-02` | 버전 가드가 중복·역전 이벤트를 걸러 최종 상태를 지킨다 |
| Q42 | 트래픽 급증과 Kafka | `MSA-03` **`KAFKA-02`** | 순간 피크는 흡수되지만 지속 초과는 랙이 무한 증가 → 앞단 레이트 리밋 필요 / **실물 브로커에서 AdminClient 로 랙 실측** |
| Q43 | 컨슈머 재처리 전략 | `MSA-04` **`KAFKA-03`** | 파티션 내 재시도 = 뒤 블로킹 / 재시도 토픽 = 전건 처리하되 순서 붕괴 (**실물 파티션에서 재현**) |
| Q40 · Q73 | 무중단 스키마 변경 | `MSA-05` **`DB-11`** | 1~4단계는 구 버전 코드가 계속 동작, 5단계(컬럼 삭제)만 되돌릴 수 없음 / **DDL 락 큐가 뒤의 일반 SELECT 까지 1.7초 세운다** |
| Q34 | 서킷브레이커 3상태 | `RES-04` **`RES-08`** | 최소 시행 횟수 유무로 OPEN 시점이 달라짐, OPEN 중 즉시 실패, HALF_OPEN 복귀 / **재시도와 겹치는 순서가 원격 호출 수와 개방 시점을 바꾼다** |
| Q44 | 커넥션 누수 판별 | `DB-06` | active/pending/usage 세 지표로 누수·장시간 점유·용량 부족을 기계적으로 구분 |
| Q45 | 오토스케일 트리거 | `MSA-03` **`KAFKA-02` `KAFKA-05`** | 컨슈머 랙이 스케일 지표로 적절함 / **파티션 수가 오토스케일 상한이라는 것을 실제 재할당으로 확인** |

### Part 6 — 대규모 RDBMS

| Q | 질문 요지 | 케이스 | 검증 내용 |
|---|---|---|---|
| Q46 | 실행계획에서 경계할 것 | `DB-03` | PostgreSQL 16 EXPLAIN 으로 Seq Scan ↔ Index Only Scan 전환을 실측 |
| **Q47** | **파티셔닝 전략** | **`DB-08`** | **파티션 키가 있으면 1개 파티션만 읽고, 없으면 전 파티션을 훑는다 / DROP PARTITION vs DELETE 의 dead tuple 차이** |
| **Q51** | **CDC 주의점** | **`DB-10`** | **논리 복제 슬롯으로 DELETE 포착 — 기본 identity 는 PK 만, FULL 이면 전체 값 / 미소비 슬롯이 WAL 을 붙잡음** |
| Q48 | 데드락 원인·회피 | `DB-07` `CON-05` | 락 순서 엇갈림 → 실패, 순서 통일 → 무사고 / ThreadMXBean 탐지 |
| Q52 · Q69 | 캐시 전략·스탬피드 | `RES-02` | sync 없으면 동시 미스만큼 원본 호출, sync=true 는 1회로 묶임 |
| Q53 | 슬로우 쿼리 프로세스 | `DB-03` (부분) | EXPLAIN 으로 접근 경로 확인 |

### Part 7 — AI 파이프라인

| Q | 질문 요지 | 케이스 | 검증 내용 |
|---|---|---|---|
| Q54 | RAG 검색 정확도 | `AI-05` | 키워드만/벡터만은 Recall@3=0.5, RRF 하이브리드는 1.0 |
| Q59 · Q79 | 기밀 유출 방지 | `AI-06` | 필터 없으면 인사 문서 유출, 후필터는 컨텍스트 공백, 전필터가 정답 |

### Part 8 — 시스템 설계

| Q | 질문 요지 | 케이스 | 검증 내용 |
|---|---|---|---|
| Q61 · Q100 | 티켓 예약 좌석 선점 · 멱등성 | `RES-01` **`DB-12`** | 원자적 키 선점으로 동시 재시도 20건 중 정확히 1건만 실행 / **DB 유니크 제약은 인스턴스가 여러 대여도 1회 보장(23505)** |
| Q62 | 분산 Rate Limit | `RES-05` | 고정 윈도 경계에서 정확히 2배 통과 / 확인·소비 비원자 시 초과 통과 |
| Q64 | URL 단축 설계 | `API-02` | Base62 자릿수 용량 계산 — **답변 원고 수정 필요(§04 문서)** |
| Q65 | 리플리케이션 vs 샤딩 | `DB-01` (부분) | 격리 수준·재읽기로 복제 지연과 같은 계열의 현상 확인 |

### Part 9 — 트러블슈팅

| Q | 질문 요지 | 케이스 | 검증 내용 |
|---|---|---|---|
| Q66 | OOM 원인 특정 | `JVM-04` `CON-04` | 강한 참조가 남으면 회수 안 됨 / ThreadLocal 누수 |
| Q67 | CPU 100% 조사 | `JVM-06` | 중첩 수량자 정규식의 지수적 시간 증가, 안전 패턴은 즉시 종료 |
| Q68 | 풀 고갈 판별 지표 | `DB-06` `DB-02` | usage 표본 유무로 누수와 슬로우 쿼리를 가름 |
| Q69 | Thundering Herd | `RES-02` | 지터·sync 로딩의 효과 |

### Part 13~16 — 클린코드·패턴·보안·백엔드 심화

| Q | 질문 요지 | 케이스 | 검증 내용 |
|---|---|---|---|
| Q92 | DI 동작 원리 | `SPRING-02` | JDK 프록시 vs CGLIB 생성 결과 |
| Q93 | 불변 객체와 방어적 복사 | `JVM-05` | record 도 가변 컬렉션을 담으면 외부에서 내용이 바뀜 |
| Q96 | Strategy 실무 적용 | `SPRING-07` | Map<빈이름, Strategy> 자동 주입 → 호출부 분기 제거 |
| Q97 | 비관적 vs 낙관적 락 | `JPA-04` `JPA-07` **`DB-09`** | 낙관적 락 예외 타입 / 비관적 락의 실제 블로킹 시간 / **둘 다 못 잡는 write skew 를 SERIALIZABLE 이 40001 로 차단** |
| Q98 | @Transactional 자기 호출 | `SPRING-01` | this 호출 시 트랜잭션 미적용 |
| Q99 | N+1 실무 해결책 | `JPA-01` `JPA-05` `JPA-06` | 1+N 실측, @BatchSize 효과, **fetch join+페이징 SQL 에 limit 없음** |
| Q100 | Exactly-Once | `RES-01` (부분) | 애플리케이션 측 멱등 쓰기로 중복 무해화 |
| Q101 | JWT 탈취 대응 | `RES-06` | 회전·재사용 탐지 → 계보 전체 무효화 |
| Q103 | OAuth 인가 코드 플로우 | `RES-07` | PKCE(S256) 없으면 코드 탈취로 토큰 획득 가능 |
| Q104 | 프리페어드 스테이트먼트 | `DB-04` | 문자열 결합은 전건 노출, 바인딩은 0건 |
| Q106 · Q107 | UUID PK 와 대안 | `DB-05` | UUIDv7 은 생성 순서 == 사전순, v4 는 아님 |
| Q108 | API 버저닝 | `API-01` | 가산적 변경은 안 깨짐(단, 클라이언트 관용 전제), 필드 삭제는 계약 위반 |

### Part 11 — MLOps

| Q | 질문 요지 | 케이스 | 검증 내용 |
|---|---|---|---|
| Q76 | Feature Store 역할 | `AI-01` | 같은 정의를 따로 구현하면 값이 어긋남(1750 vs 1500), 공통 정의는 일치 |
| Q77 | 모델 드리프트 | `AI-02` | PSI 가 변화 크기에 단조 증가, 임계치 0.25 초과 시 경보 |
| Q78 | ANN 이 필수인 이유 | `AI-03` | nprobe 를 넓힐수록 재현율↑ 계산량↑ |
| Q113 | 임베딩 차원 수 | `AI-04` | int8 양자화 메모리 1/4·재현율 유지 / 학습 안 된 임베딩의 단순 절단은 붕괴 |

### 기타 (질문 번호와 1:1은 아니지만 답변 근거가 되는 케이스)

`SPRING-03` 전파, `SPRING-04` 롤백 규칙, `SPRING-05` 빈 스코프, `SPRING-06` @Async 컨텍스트,
`JPA-02` 변경 감지, `JPA-03` OSIV, `CON-01`~`CON-03` 동시성, `JVM-01`~`JVM-03` 자바 기본기,
`RES-03` 백오프·지터.

---

## B. 이론 전용 — 검증 케이스를 만들지 않은 질문

코드로 참/거짓이 갈리지 않는 질문들이다. 억지로 케이스를 만들면 "검증한 척"이 되므로 만들지 않았다.

| Q | 이유 |
|---|---|
| Q32 REST vs gRPC | 선택 기준 설명형. 다만 "L4 LB 에서 HTTP/2 연결 고정" 은 C 분류(실측 필요) |
| Q36 API 게이트웨이 역할 | 아키텍처 책임 배분 |
| Q38 컨테이너 vs VM | 개념 비교 |
| Q39 K8s 도입 이점 | 개념 + 조직 판단 |
| Q49 정규화/비정규화 | 설계 판단 |
| Q50 OLTP/OLAP 분리 | 설계 판단 |
| Q55 청킹 | 알고리즘 검증은 가능하나 품질 판정에 사람 평가가 필요 |
| Q56 추론 API 레이턴시 | C 분류(LLM API 필요) |
| Q57 ETL vs ELT | 설계 판단 |
| Q63 SPOF 특정 | 방법론 |
| Q71 Observability 3본주 | 개념 |
| Q72 CI/CD | 프로세스 |
| Q75 IaC | 프로세스 |
| Q80 배치→실시간 추론 | 사상 전환 설명 |
| Q81~Q90 리더십 10문항 | 조직·매니지먼트 |
| Q91 SOLID | 설계 원칙 |
| Q94 DRY/KISS/YAGNI | 설계 원칙 |
| Q95 DDD | 설계 원칙 |
| Q111 프롬프트 vs RAG 순서 | 판단 순서 |
| Q112 Spring AI 통합 | 조직·스택 판단 |
| Q114 의도적 기술 부채 | 경험담 |
| Q115 커리어 | 경험담 |

## C. 인프라가 있어야 진짜 검증이 되는 질문 (확장 후보)

| Q | 필요한 것 | 만들면 무엇을 확인할 수 있나 |
|---|---|---|
| Q33 분산 트레이싱 | OpenTelemetry + Collector | Trace ID 전파, 로그와 트레이스 결합 |
| Q35 mTLS | 인증서 + Istio | 인증서 없는 프로세스는 통신 불가 |
| ~~Q41 Kafka vs RabbitMQ~~ | ~~Kafka~~ | **해소됨** — `KAFKA-01` 이 실물 브로커에서 파티션 단위 순서 보증과 오프셋 되감기를 확인한다 |
| ~~Q46 실행계획~~ | ~~PostgreSQL~~ | **해소됨** — `DB-03` 이 PostgreSQL 16 에서 계획 전환을 실측한다. 추정 행수 vs 실측 행수 괴리는 `EXPLAIN ANALYZE` 로 확장 가능 |
| ~~Q47 파티셔닝~~ | ~~PostgreSQL~~ | **해소됨** — `DB-08` 이 프루닝과 DROP PARTITION 비용을 실측한다 |
| ~~Q51 CDC~~ | ~~Debezium + Kafka~~ | **해소됨** — `DB-10` 이 PostgreSQL 논리 복제 슬롯만으로 삭제 포착과 슬롯 적체를 확인한다. Debezium 자체(오프셋 관리·스키마 레지스트리)는 여전히 Kafka 필요 |
| Q56 · Q111~Q113 | LLM API / 임베딩 모델 | SSE 첫 토큰 지연, 시맨틱 캐시 적중률 |
| Q58 데이터 품질 | dbt / Great Expectations | 4계층 검증 + 데이터 서킷 브레이커 |
| Q60 로컬 LLM | GPU | 손익분기점 시산 |
| Q70 · Q74 · Q110 | Kubernetes | graceful shutdown + preStop, 프로브 오설정 재시작 루프 (**리밸런스는 `KAFKA-05` 로 해소**) |
| Q102 CORS | 브라우저 또는 MockMvc | Preflight 요구 조건, 비브라우저 클라이언트는 무관 |
| Q109 Webhook | HTTP 서버 | 서명 검증 + 재시도 + 멱등 수신 |

> 이 랩이 띄우는 인프라는 **PostgreSQL 16 하나뿐**이다(`compose.yaml`). 위 항목은 `verify-core` 를 그대로 쓰고
> 별도 모듈(예: `verify-labs-kafka`)로 붙이는 것을 전제로 남겨 두었다 — compose 에 서비스를 한 줄 더 얹으면 된다.
